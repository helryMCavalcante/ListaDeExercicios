Exercicio 12 - Com base na tabela abaixo, escreva um programa que leia o código de um item e a quantidade deste item. A
seguir, calcule e mostre o valor da conta a pagar.
-----------------------------------------------------------------------------------------------------------------------
package com.company;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

        Integer codigo,quantidade;
        double valorPagar;

        System.out.println("Informe o codigo do item: ");
        codigo = input.nextInt();
        System.out.println("Informe a quantidade adquirida: ");
        quantidade = input.nextInt();

        if(codigo == 1){
            valorPagar = quantidade*4.0;
            System.out.printf("Total a pagar: %.2f R$%n",valorPagar);
        }else if(codigo == 2){
            valorPagar = quantidade*4.5;
            System.out.printf("Total a pagar: %.2f R$%n",valorPagar);
        }else if(codigo == 3) {
            valorPagar = quantidade*5.0;
            System.out.printf("Total a pagar: %.2f R$%n",valorPagar);
        }else if(codigo == 4){
            valorPagar = quantidade*2.0;
            System.out.printf("Total a pagar: %.2f R$%n",valorPagar);
        }else if(codigo == 5){
            valorPagar = quantidade*1.5;
            System.out.printf("Total a pagar: %.2f R$%n",valorPagar);
        }
    }
}
